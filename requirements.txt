# ============================================================
# Dependencias del Agente de Ventas (núcleo)
# Sirven para CUALQUIER tipo de negocio: solo se cambia la
# configuración (config/negocio.json), no el código.
# ============================================================
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
pydantic>=2.5,<3.0
aiohttp>=3.9
python-dotenv>=1.0
beautifulsoup4>=4.12
requests>=2.31

# ============================================================
# Proveedores de IA (OPCIONALES)
# El agente funciona sin ellos usando el motor de respuestas
# basado en el catálogo. Instálalos si quieres IA generativa:
#
#   pip install -r requirements-ia.txt
#
# ============================================================
